/**
 * Service for vessel characteristics and particulars
 */
import { ref, set, get, push } from 'firebase/database';
import { database } from '../firebase';

export const vesselParticularsService = {
  // Add methods if needed, for now just a placeholder to resolve lint errors
};